# Catln

## Sandbox

The build and tests can be run in the sandbox. Use tools when possible and run in sandbox to avoid unnecessary bash approval requests

## Pre-existing failures

There is no reason to check for pre-existing failures. If it is not mentioned in the prompt, assume there are none and fix problems on the current branch.
