# Tests higher order functions

data T(x: Integer)

:T(x).tst(f(y -> Integer) -> Boolean) -> Boolean
:T(x).tst(f(y -> Integer) -> Boolean) = f(y=x)

main{io: IO} =
  # #assert(test= T(0).tst(f(y)= y == 0))
  io

